import { StyleSheet } from "react-native";

export const errorStyles = StyleSheet.create({
  errorContainer: {
    backgroundColor: "#ffebee",
    padding: 10,
    borderRadius: 8,
    borderWidth: 1,
    borderColor: "#ffcdd2",
  },
  errorText: {
    color: "#c62828",
    fontSize: 14,
  },
  fieldError: {
    fontSize: 12,
    color: "#FF4444",
    marginTop: 4,
  },
});
